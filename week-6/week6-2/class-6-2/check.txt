import React, { useState } from "react"; // Import useState from React

function App() {
  const [inputValue, setInputValue] = useState(0); // Define state variables

  return (
    <div>
      <input
        type="text"
        onChange={function (e) {
          setInputValue(e.target.value); // Update state on input change
        }}
      />
    </div>
  );
}

export default App;
*/
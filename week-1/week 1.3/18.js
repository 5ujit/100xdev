// Example usage of various Math methods
/*
let x = -4.5;
let y = 2.7;
let z = 3;

// Math.abs(x) - Returns the absolute value of x
console.log("Absolute value of x:", Math.abs(x));

// Math.ceil(x) - Returns x, rounded upwards to the nearest integer
console.log("Ceiling of x:", Math.ceil(x));

// Math.floor(x) - Returns x, rounded downwards to the nearest integer
console.log("Floor of x:", Math.floor(x));

// Math.round(x) - Rounds x to the nearest integer
console.log("Rounded value of x:", Math.round(x));

// Math.max(x, y, z, ..., n) - Returns the number with the highest value
console.log("Maximum value among x, y, and z:", Math.max(x, y, z));

// Math.min(x, y, z, ..., n) - Returns the number with the lowest value
console.log("Minimum value among x, y, and z:", Math.min(x, y, z));

// Math.pow(x, y) - Returns the value of x to the power of y
console.log("Value of x raised to the power of y:", Math.pow(x, y));

// Math.sqrt(x) - Returns the square root of x
console.log("Square root of x:", Math.sqrt(x));

// Math.random() - Returns a random number between 0 and 1
console.log("Random number between 0 and 1:", Math.random());

// Math.sin(x) - Returns the sine of x (x is in radians)
console.log("Sine of x:", Math.sin(x));

// Math.cos(x) - Returns the cosine of x (x is in radians)
console.log("Cosine of x:", Math.cos(x));

// Math.tan(x) - Returns the tangent of x (x is in radians)
console.log("Tangent of x:", Math.tan(x));
console.log("Maximum value among x, y, and z:", Math.max(x, y, z));

*/

function objectMethods(obj){
  console.log("Original Object:", obj);

  let keys = Object.keys(obj)
}